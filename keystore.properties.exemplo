# Copie este arquivo para "keystore.properties" (mesma pasta) e preencha.
# Use apenas se for gerar o app pelo Android Studio sem o assistente de assinatura.
storeFile=upload-keystore.jks
storePassword=SUA_SENHA
keyAlias=upload
keyPassword=SUA_SENHA
